import React from 'react';
import './selected_filters.css'

function SelectedFilters () {
    return (
        <div className='selected-filters'>
            selected filters for test
        </div>
    );
}

export default SelectedFilters;
